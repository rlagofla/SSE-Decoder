这个 FAST 消息比较长，所以Pmap也比较长，我目前发现需要7个字节，所以估计要用uint64来存储pmap


- pmap
- template id
- tick time
- data status
- security id：none类型，在位示图中不存在，但是一定会在消息体中
- ImageStatus：也是none类型
- PreClosePx
- OpenPx
- HighPx
- LowPx
- LastPx
- ClosePx
- InstrumentStatus
- TradingPhaseCode
- NumTrades
- TotalVolumeTrade
- TotalValueTrade
- TotalBidQty
- WeightedAvgBidPx
- AltWeightedAvgBidPx
- TotalOfferQty
- WeightedAvgOfferPx
- AltWeightedAvgOfferPx
- IOPV
- ETFBuyNumber
- ETFBuyAmount
- ETFBuyMoney
- ETFSellNumber
- ETFSellAmount
- ETFSellMoney
- YieldToMaturity
- TotalWarrantExecQty
- WarLowerPx
- WarUpperPx
- WithdrawBuyNumber
- WithdrawBuyAmount
- WithdrawBuyMoney
- WithdrawSellNumber
- WithdrawSellAmount
- WithdrawSellMoney
- TotalBidNumber
- TotalOfferNumber
- BidTradeMaxDuration
- OfferTradeMaxDuration
- NumBidOrders
- NumOfferOrders
- NoBidLevel: none 类型，pmap不存在，一定会在消息体中，此字段会引发一个循环，循环NoBidLevel次
    - pmap：这个pmap定义了下面的字段
    - PriceLevelOperator：废用了，占用位图，但不应该出现
    - Price
    - OrderQty
    - NumOrders
    - Orders: 又是个none，在这个子pmap中不存在，然后引发下一个循环，循环Orders次：
        - pmap
        - OrderQueueOperator：废用了，占用位图，但不应该出现
        - OrderQueueOperatorEntryID：废用了，占用位图，但不应该出现
        - OrderQty
- NoOfferLevel: none 类型，pmap不存在，一定会在消息体中，此字段会引发一个循环，循环NoOfferLevel次
    - pmap：这个pmap定义了下面的字段
    - PriceLevelOperator：废用了，占用位图，但不应该出现
    - Price
    - OrderQty
    - NumOrders
    - Orders: 又是个none，在这个子pmap中不存在，然后引发下一个循环，循环Orders次：
        - pmap
        - OrderQueueOperator：废用了，占用位图，但不应该出现
        - OrderQueueOperatorEntryID：废用了，占用位图，但不应该出现
        - OrderQty

